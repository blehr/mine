jQuery( document ).ready( function( $ ) {

  $('input[type="text"], input[type="search"], input[type="email"], input[type="url"], textarea, select').addClass('form-control');

  $('input[type="submit"]').addClass('btn btn-default');

  $('input[type="button"], button').addClass('btn btn-default');

  $('.widget ul').addClass('list-unstyled');

});
